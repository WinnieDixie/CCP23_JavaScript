operation = user.input
a = user.input
b = user.input
answer = 
if operation = addition,
 then answer = a + b;
 if operation = substration,
    then answer = a - b;
 if operation = mutiplation,
    then answer = a * b;
 if operation = division,
    then answer = a / b;
        
